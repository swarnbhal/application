import { users } from "@/data/users"
import { messages } from "@/data/messages"
import { threads } from "@/data/threads"
import { activityEvents } from "@/data/activity"
import { seedInsights } from "@/data/insights"

export const fixtureUsers = users
export const fixtureMessages = messages
export const fixtureThreads = threads
export const fixtureActivity = activityEvents
export const fixtureInsights = seedInsights
